#include "menu.hpp"
using namespace std;
int main(int argc, char* argv[]){
    //creating an object of main menu class where the for Main manu is encapsulated
    MainMenu obj;
    obj.run();
    return 0;
}